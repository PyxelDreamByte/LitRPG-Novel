# Character Profiles

Create one authoritative profile per character inside the owning work using `../templates/character-profile.md`. Preserve stable work-bound character IDs even if names, titles, bodies, roles, or public identities change.

Separate private truth from public record, personal belief, interface presentation, and reader disclosure.
