# Character Profiles

Create one authoritative profile per character using `../templates/character-profile.md`. Preserve stable character IDs even if names, titles, bodies, roles, or public identities change.

Separate private truth from public record, personal belief, interface presentation, and reader disclosure.

