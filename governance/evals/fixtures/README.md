# Evaluation Fixtures

Place deliberately flawed, synthetic artifacts here. Fixtures are non-canonical and must use names and facts that cannot be mistaken for live story material.

Prefer one fault per minimal fixture, followed by mixed-fault integration fixtures. Every accepted repair should gain a regression fixture.

