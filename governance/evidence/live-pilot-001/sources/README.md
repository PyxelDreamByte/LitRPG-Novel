# Retained source snapshot

`world-canon-auditor.toml` preserves the exact bytes of the
`.codex/agents/world-canon-auditor.toml` input evaluated by
`live-pilot-001`. The active agent definition later changed, so the evidence
manifest now points to this archival copy while retaining the original
content-pinned revision label and SHA-256 digest.
